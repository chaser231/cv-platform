# 🤖 Настройка Replicate AI для CV Platform

## Шаг 1: Получение API ключа

1. Перейдите на https://replicate.com
2. Войдите или зарегистрируйтесь
3. Перейдите в **Account → API Tokens**: https://replicate.com/account/api-tokens
4. Нажмите **"Create token"**
5. Скопируйте токен (начинается с `r8_...`)

## Шаг 2: Добавление карты (вы уже сделали ✅)

Billing: https://replicate.com/account/billing

## Шаг 3: Настройка .env.local

```bash
# Скопируйте env.example
cp env.example .env.local

# Откройте и отредактируйте
nano .env.local
```

**Измените эти строки:**

```bash
# Вставьте ваш API ключ
VITE_REPLICATE_API_KEY=r8_ваш_ключ_здесь

# Включите реальный AI (выключите моки)
VITE_AI_MOCK_MODE=false
```

## Шаг 4: Проверка работы

```bash
# Запустите приложение
npm run dev
```

Откройте http://localhost:5173 и попробуйте:
1. Перейти в "Обо мне (Summary)"
2. Нажать "Переписать профессионально"
3. AI должен улучшить текст

---

## 📊 Доступные модели

### Рекомендуемые для резюме:

| Модель | Скорость | Качество | Цена | Когда использовать |
|--------|----------|----------|------|-------------------|
| **llama-8b** | ⚡⚡⚡ | ⭐⭐⭐⭐ | $0.05/$0.25 | Default, большинство задач |
| **qwen-7b** | ⚡⚡⚡ | ⭐⭐⭐⭐ | $0.05/$0.25 | Русский язык, перевод |
| **llama-70b** | ⚡⚡ | ⭐⭐⭐⭐⭐ | $0.65/$2.75 | Cover Letter, сложные задачи |
| **mistral-7b** | ⚡⚡⚡ | ⭐⭐⭐⭐ | $0.05/$0.25 | Альтернатива llama-8b |

### Цены (за 1M токенов):

- **llama-8b**: Input $0.05, Output $0.25
- **llama-70b**: Input $0.65, Output $2.75
- **qwen-7b**: Input $0.05, Output $0.25

### Расчёт стоимости:

Средний запрос (улучшение Summary):
- Input: ~300 токенов × $0.05/1M = $0.000015
- Output: ~200 токенов × $0.25/1M = $0.00005
- **Итого: ~$0.000065 за запрос**

**1000 запросов = ~$0.065** 🎉

---

## 🔧 Конфигурация моделей

В файле `src/services/ai/aiRouter.js` настроено:

```javascript
// Простые задачи → дешёвые модели
'improve_bullet': llama-8b

// Сложные задачи → качественные модели  
'cover_letter': llama-70b
'gap_analysis': llama-70b

// Мультиязычность → qwen
'translate': qwen-7b
```

---

## 🐛 Troubleshooting

### "Replicate provider is not configured"

```bash
# Проверьте .env.local
cat .env.local | grep REPLICATE
# Должно быть:
# VITE_REPLICATE_API_KEY=r8_...
# VITE_REPLICATE_ENABLED=true
```

### "Prediction timeout"

Увеличьте таймаут в `.env.local`:
```bash
VITE_AI_TIMEOUT_MS=90000
```

### "Failed to create prediction: 401"

Неверный API ключ. Проверьте на https://replicate.com/account/api-tokens

### "Model not found"

Модель могла быть обновлена. Проверьте актуальные версии:
https://replicate.com/meta/meta-llama-3.1-8b-instruct

---

## 📈 Мониторинг использования

Replicate Dashboard: https://replicate.com/account/billing

Там видно:
- Количество запросов
- Потраченные деньги
- История predictions

---

## 🔒 Безопасность

⚠️ **Никогда не коммитьте .env.local в git!**

Файл уже добавлен в `.gitignore`.

Для production используйте переменные окружения хостинга (Vercel, Netlify).

---

## ✅ Чеклист

- [ ] Получен API ключ Replicate
- [ ] Добавлена карта оплаты
- [ ] Создан `.env.local`
- [ ] Вставлен API ключ
- [ ] `VITE_AI_MOCK_MODE=false`
- [ ] Приложение запущено
- [ ] AI работает (проверен Summary Rewriter)

---

*Готово! Теперь AI работает через Replicate* 🚀

