# Руководство по настройке проекта CV Platform

## 📋 Содержание

1. [Системные требования](#системные-требования)
2. [Установка Node.js](#установка-nodejs)
3. [Установка проекта](#установка-проекта)
4. [Структура проекта](#структура-проекта)
5. [Запуск приложения](#запуск-приложения)
6. [Возможные проблемы](#возможные-проблемы)

---

## Системные требования

### Минимальные требования:
- **Операционная система**: Windows 10+, macOS 10.15+, Linux (Ubuntu 20.04+)
- **Node.js**: версия 18.x или выше
- **npm**: версия 9.x или выше (устанавливается вместе с Node.js)
- **RAM**: минимум 4GB
- **Свободное место на диске**: 500MB

### Рекомендуемые требования:
- **Node.js**: версия 20.x
- **RAM**: 8GB или больше
- **Браузер**: Chrome/Edge 100+, Firefox 100+, Safari 15+

---

## Установка Node.js

### macOS

#### Вариант 1: Homebrew (рекомендуется)
```bash
# Установка Homebrew (если еще не установлен)
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"

# Установка Node.js
brew install node
```

#### Вариант 2: Официальный установщик
1. Скачайте с [nodejs.org](https://nodejs.org/)
2. Выберите LTS версию (Long Term Support)
3. Запустите установщик и следуйте инструкциям

### Windows

#### Вариант 1: Официальный установщик (рекомендуется)
1. Скачайте с [nodejs.org](https://nodejs.org/)
2. Выберите Windows Installer (.msi) LTS версию
3. Запустите и установите с настройками по умолчанию

#### Вариант 2: Chocolatey
```powershell
choco install nodejs-lts
```

### Linux (Ubuntu/Debian)

```bash
# Обновление системы
sudo apt update

# Установка Node.js 20.x
curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -
sudo apt-get install -y nodejs

# Проверка установки
node --version
npm --version
```

### Проверка установки

После установки Node.js откройте терминал и выполните:

```bash
node --version
# Должно вывести: v18.x.x или выше

npm --version
# Должно вывести: 9.x.x или выше
```

---

## Установка проекта

### Шаг 1: Получение кода

Если проект в Git репозитории:
```bash
git clone <repository-url>
cd "CV Platform"
```

Если у вас есть архив:
```bash
# Распакуйте архив и перейдите в папку
cd "CV Platform"
```

### Шаг 2: Установка зависимостей

```bash
npm install
```

Этот процесс может занять 2-5 минут. В процессе будут установлены:
- React и React DOM
- Vite (build tool)
- Tailwind CSS
- Lucide React (иконки)
- ESLint (линтер)
- И другие dev-зависимости

**Ожидаемый вывод:**
```
added 245 packages, and audited 246 packages in 2m

42 packages are looking for funding
  run `npm fund` for details

found 0 vulnerabilities
```

---

## Структура проекта

После установки структура должна выглядеть так:

```
CV Platform/
├── node_modules/          # Установленные зависимости (не коммитится)
├── public/                # Статические файлы
│   └── vite.svg          # Иконка приложения
├── src/                   # Исходный код
│   ├── main.jsx          # Entry point приложения
│   └── index.css         # Глобальные стили с Tailwind
├── docs/                  # Документация
│   ├── CONTEXT.md        # Контекст проекта
│   ├── RULES.md          # Правила разработки
│   ├── ROADMAP.md        # План развития
│   └── SETUP_GUIDE.md    # Этот файл
├── App.jsx               # Главный компонент
├── index.html            # HTML шаблон
├── package.json          # Конфигурация и зависимости
├── vite.config.js       # Настройки Vite
├── tailwind.config.js   # Настройки Tailwind CSS
├── postcss.config.js    # Настройки PostCSS
├── .eslintrc.cjs        # Настройки ESLint
├── .gitignore           # Игнорируемые файлы для Git
├── env.example          # Пример файла переменных окружения
└── README.md            # Основная документация
```

---

## Запуск приложения

### Development режим (разработка)

```bash
npm run dev
```

**Результат:**
```
  VITE v5.4.10  ready in 324 ms

  ➜  Local:   http://localhost:3000/
  ➜  Network: use --host to expose
  ➜  press h + enter to show help
```

Приложение автоматически откроется в браузере по адресу `http://localhost:3000`

### Особенности dev режима:
- **Hot Module Replacement (HMR)**: Изменения в коде мгновенно отображаются без перезагрузки
- **Source maps**: Легкая отладка в DevTools
- **Быстрый старт**: Vite запускается за секунды

### Production build (сборка для продакшена)

```bash
npm run build
```

Создаст оптимизированную версию в папке `dist/`

### Preview production build

```bash
npm run preview
```

Запустит локальный сервер для просмотра production сборки

---

## Возможные проблемы

### Проблема 1: `npm install` выдает ошибки

**Симптомы:**
```
npm ERR! code EACCES
npm ERR! syscall access
npm ERR! path /usr/local/lib/node_modules
```

**Решение (macOS/Linux):**
```bash
# Исправление прав доступа
sudo chown -R $(whoami) ~/.npm
sudo chown -R $(whoami) /usr/local/lib/node_modules
```

**Решение (Windows):**
- Запустите терминал от имени администратора

---

### Проблема 2: Порт 3000 уже занят

**Симптомы:**
```
Port 3000 is in use
```

**Решение:**

Вариант 1 - Изменить порт в `vite.config.js`:
```javascript
server: {
  port: 3001, // Измените на свободный порт
}
```

Вариант 2 - Убить процесс на порту 3000:
```bash
# macOS/Linux
lsof -ti:3000 | xargs kill -9

# Windows
netstat -ano | findstr :3000
taskkill /PID <PID> /F
```

---

### Проблема 3: Tailwind стили не применяются

**Симптомы:**
- Приложение запускается, но выглядит без стилей
- В консоли нет ошибок

**Решение:**
1. Проверьте, что `src/index.css` импортирован в `src/main.jsx`
2. Убедитесь, что в `tailwind.config.js` указаны правильные пути к файлам
3. Перезапустите dev сервер (`Ctrl+C` и снова `npm run dev`)

---

### Проблема 4: Ошибки при импорте иконок Lucide

**Симптомы:**
```
Module not found: Can't resolve 'lucide-react'
```

**Решение:**
```bash
# Переустановите зависимости
rm -rf node_modules package-lock.json
npm install
```

---

### Проблема 5: React hooks ошибки

**Симптомы:**
```
Error: Invalid hook call. Hooks can only be called inside...
```

**Решение:**
- Убедитесь, что используется только одна версия React
```bash
npm ls react
```
- Если версий несколько, очистите и переустановите:
```bash
rm -rf node_modules package-lock.json
npm install
```

---

## Команды для устранения проблем

### Полная переустановка зависимостей

```bash
# Удаление node_modules и package-lock.json
rm -rf node_modules package-lock.json

# Очистка npm cache
npm cache clean --force

# Установка заново
npm install
```

### Проверка устаревших пакетов

```bash
npm outdated
```

### Обновление всех пакетов (осторожно!)

```bash
npm update
```

---

## Полезные команды

### Проверка кода линтером

```bash
npm run lint
```

### Автоматическое исправление lint ошибок

```bash
npm run lint -- --fix
```

### Просмотр размера bundle

```bash
npm run build
ls -lh dist/assets/
```

---

## IDE Setup (опционально)

### VS Code

Рекомендуемые расширения:

1. **ES7+ React/Redux/React-Native snippets** - сниппеты для React
2. **Tailwind CSS IntelliSense** - автодополнение Tailwind классов
3. **ESLint** - интеграция линтера
4. **Prettier** - форматирование кода
5. **Auto Rename Tag** - автоматическое переименование парных тегов

Установка:
```bash
code --install-extension dsznajder.es7-react-js-snippets
code --install-extension bradlc.vscode-tailwindcss
code --install-extension dbaeumer.vscode-eslint
```

### WebStorm/IntelliJ IDEA

- Tailwind CSS support уже встроен
- Включите ESLint: Settings → Languages & Frameworks → JavaScript → Code Quality Tools → ESLint

---

## Следующие шаги

После успешной установки:

1. **Изучите документацию:**
   - [CONTEXT.md](./CONTEXT.md) - Концепции и архитектура
   - [RULES.md](./RULES.md) - Правила разработки
   - [ROADMAP.md](./ROADMAP.md) - План развития

2. **Начните разработку:**
   - Откройте `App.jsx` и изучите структуру
   - Попробуйте изменить текст и посмотрите на hot reload
   - Изучите компоненты (Sidebar, EditorForm, ResumePreview)

3. **Добавьте фичи:**
   - См. TODO в [ROADMAP.md](./ROADMAP.md)
   - Следуйте code style из [RULES.md](./RULES.md)

---

## Дополнительные ресурсы

- [React документация](https://react.dev/)
- [Vite документация](https://vitejs.dev/)
- [Tailwind CSS документация](https://tailwindcss.com/docs)
- [Lucide иконки](https://lucide.dev/)

---

**Нужна помощь?** Создайте issue в репозитории или обратитесь к документации проекта.





