# Руководство для ИИ-ассистента по работе с CV Platform

## 🤖 Введение

Этот документ создан специально для AI-ассистентов (Claude, ChatGPT, Cursor AI и др.), которые будут помогать в разработке CV Platform.

---

## 📖 Обязательное чтение перед началом работы

### 1. Первый запрос от пользователя - прочитай:

```
CONTEXT.md     → Понимание проекта, концепций, архитектуры
RULES.md       → Code style, best practices, соглашения
ROADMAP.md     → Текущие задачи, приоритеты, план развития
```

### 2. При работе с кодом - изучи:

```
App.jsx        → Текущая реализация, структура компонентов
package.json   → Доступные зависимости и скрипты
```

### 3. При установке/проблемах - используй:

```
SETUP_GUIDE.md → Детальное решение технических проблем
```

---

## 🎯 Ключевые концепции (MUST KNOW)

### 1. Мастер-профиль (Master Profile)

**Что это:**
- Единый источник всех данных пользователя
- Редактируется один раз
- Используется для генерации множества адаптированных версий

**Структура данных:**
```javascript
{
  personalInfo: { fullName, title, email, phone, location, links },
  summary: string,
  experience: [{ id, company, role, dates, description }],
  skills: string[],
  education: [{ id, institution, degree, dates }]
}
```

**Почему важно:**
Пользователь НЕ создает резюме "с нуля" каждый раз. Он заполняет мастер-профиль ОДИН РАЗ, а потом адаптирует его под разные вакансии.

### 2. Job Tailoring (Адаптация под вакансию)

**Процесс:**
1. Пользователь вставляет Job Description (текст вакансии)
2. AI анализирует требования
3. Извлекаются ключевые навыки
4. Генерируется адаптированная версия резюме

**Зачем:**
Прохождение ATS фильтров (Applicant Tracking Systems) - автоматических систем, которые сканируют резюме на наличие ключевых слов из вакансии.

### 3. XYZ Формула

**Формат:**
```
"Accomplished [X] as measured by [Y], by doing [Z]"
```

**Пример:**
❌ Плохо: "Разрабатывал интерфейсы для CRM"
✅ Хорошо: "Reduced page load time by 40% (LCP) by implementing React lazy loading and code splitting"

**Применение:**
AI берет простое описание опыта и превращает его в формат с измеримыми результатами.

### 4. ATS (Applicant Tracking System)

**Проблема:**
80% резюме отсеиваются автоматически, не доходя до HR.

**Решение:**
Платформа анализирует JD и добавляет релевантные ключевые слова в резюме.

**Пример:**
Вакансия требует "React, TypeScript, Redux" → AI проверит, что эти слова есть в резюме.

### 5. Split-View Context (Концепция UX)

**Что это:**
Вместо жесткого пошагового визарда — двухпанельный редактор:

- **Левая панель (Input)**: Формы редактирования, AI-кнопки, чат-ассистент
- **Правая панель (Preview)**: Live render PDF, мгновенное обновление (<500ms)

**Interactive Mode:**
Клик по блоку в PDF → автоматический скролл к полю ввода слева

**Command Bar (Cmd+K):**
Быстрый доступ к AI-функциям через keyboard shortcut

### 6. Language Switcher (RU ↔ EN)

**Критически важно:**
- Платформа ОБЯЗАНА поддерживать RU и EN
- Все AI промпты должны иметь переменную `locale: 'ru' | 'en'`
- AI генерирует строго на выбранном языке
- Переключатель языка → AI переводит контент с сохранением форматирования

### 7. PII Masking (Безопасность)

**Проблема:**
Отправка личных данных в LLM может нарушить приватность

**Решение:**
Перед отправкой в AI:
- Email → `[PII_EMAIL]`
- Phone → `[PII_PHONE]`
- Address → `[PII_ADDRESS]`

После получения ответа → токены заменяются обратно

### 8. Watermark Growth (Маркетинг)

**Стратегия:**
- Free план → PDF с watermark "Made with CVPlatform.io"
- Создает viral loop через рекрутеров
- Conversion в Pro: когда пользователь видит результат

---

## 📏 Правила кодирования (CODE STYLE)

### Именование

```javascript
// ✅ ПРАВИЛЬНО
const ResumePreview = () => {}        // Компоненты: PascalCase
const handleAIAction = () => {}       // Функции: camelCase
const API_BASE_URL = "..."            // Константы: UPPER_SNAKE_CASE

// ❌ НЕПРАВИЛЬНО
const resumePreview = () => {}
const HandleAIAction = () => {}
const apiBaseUrl = "..."
```

### Компоненты

```javascript
// ✅ ТОЛЬКО функциональные компоненты
const Component = ({ prop1, prop2 }) => {
  const [state, setState] = useState(initial);
  
  useEffect(() => {
    // side effects
  }, [dependencies]);
  
  return <div>...</div>;
};

// ❌ НЕТ классовых компонентов
class Component extends React.Component { ... }
```

### Стилизация

```javascript
// ✅ Tailwind-first
<div className="flex items-center space-x-2 p-4 bg-slate-900">

// ✅ Условные стили
<div className={`base ${isActive ? 'bg-blue-600' : 'bg-slate-800'}`}>

// ❌ Избегать inline styles (кроме динамики)
<div style={{ padding: '10px' }}>  // НЕТ
<div style={{ color: userColor }}>  // ОК для динамических значений
```

### Состояние (State Management)

```javascript
// ✅ Immutable обновления
setProfileData(prev => ({
  ...prev,
  summary: newSummary
}));

// ❌ Мутации
profileData.summary = newSummary;  // НИКОГДА!
```

---

## 🚦 Workflow для разных типов задач

### Тип 1: Добавление новой фичи

**Шаги:**
1. Прочитай ROADMAP.md - убедись, что фича в плане
2. Прочитай RULES.md - изучи соглашения
3. Изучи App.jsx - пойми текущую структуру
4. Создай компонент согласно code style
5. Интегрируй в существующую структуру
6. Обнови ROADMAP.md (отметь как completed)

**Пример запроса:**
> "Добавь возможность удаления блоков опыта работы"

**Твои действия:**
1. Найди компонент EditorForm в App.jsx
2. Найди секцию experience
3. Добавь кнопку с иконкой Trash2
4. Добавь обработчик удаления (immutable!)
5. Проверь, что превью обновляется

### Тип 2: Исправление бага

**Шаги:**
1. Воспроизведи проблему (пойми контекст)
2. Найди причину в коде
3. Исправь, следуя RULES.md
4. Убедись, что не сломал другое

**Пример запроса:**
> "При изменении summary превью не обновляется"

**Твои действия:**
1. Проверь, что onChange вызывается
2. Проверь, что state обновляется immutably
3. Проверь, что ResumePreview получает новый data prop

### Тип 3: Рефакторинг

**Шаги:**
1. Прочитай RULES.md секцию "Tech Debt"
2. Согласуй масштаб рефакторинга с пользователем
3. Рефактори по частям (не всё сразу)
4. Проверь, что функциональность не сломалась

**Пример запроса:**
> "Вынеси компоненты из App.jsx в отдельные файлы"

**Твои действия:**
1. Создай структуру /src/components
2. Начни с простых компонентов (Sidebar, AIGenerateButton)
3. Один компонент = один файл
4. Обнови импорты в App.jsx

### Тип 4: Установка и конфигурация

**Шаги:**
1. Используй SETUP_GUIDE.md как справочник
2. Объясни шаги простым языком
3. Предвиди возможные проблемы
4. Дай troubleshooting инструкции

**Пример запроса:**
> "Проект не запускается, ошибка о порте"

**Твои действия:**
1. См. SETUP_GUIDE.md → "Проблема 2: Порт занят"
2. Предложи изменить vite.config.js
3. Или дай команду убить процесс

---

## 🎨 AI Features (специфика проекта)

### Текущая реализация (MVP)

**Моки с setTimeout:**
```javascript
const handleAIAction = () => {
  setIsProcessing(true);
  setTimeout(() => {
    setIsProcessing(false);
    // Update data
  }, 1500);
};
```

**Когда пользователь просит "подключить AI":**
1. Объясни, что нужен API ключ (OpenAI/Claude)
2. Покажи, как создать `/api` endpoint
3. Замени setTimeout на реальный fetch
4. Добавь обработку ошибок

### Будущая реализация

**Структура промпта для Summary:**
```
System: You are a professional resume writer.

User: Rewrite this summary professionally:
"{user_summary}"

Job context: {job_description_keywords}

Requirements:
- Keep it under 150 words
- Highlight relevant experience
- Use action verbs
- Include metrics if possible
```

**Куда писать промпты:**
```
/src/prompts/
  summary.js
  experience.js
  jobAnalysis.js
```

---

## 🔍 Частые вопросы от пользователей

### "Добавь экспорт в PDF"

**Решение:**
```bash
npm install html2pdf.js
```

```javascript
import html2pdf from 'html2pdf.js';

const exportToPDF = () => {
  const element = document.getElementById('resume-preview');
  const opt = {
    margin: 0,
    filename: `${profileData.personalInfo.fullName}_CV.pdf`,
    image: { type: 'jpeg', quality: 0.98 },
    html2canvas: { scale: 2 },
    jsPDF: { unit: 'mm', format: 'a4', orientation: 'portrait' }
  };
  html2pdf().set(opt).from(element).save();
};
```

См. RULES.md раздел "Экспорт в PDF" для деталей.

### "Добавь сохранение в LocalStorage"

**Решение:**
```javascript
// При изменении profileData
useEffect(() => {
  localStorage.setItem('cv_master_profile', JSON.stringify(profileData));
}, [profileData]);

// При загрузке
useEffect(() => {
  const saved = localStorage.getItem('cv_master_profile');
  if (saved) {
    setProfileData(JSON.parse(saved));
  }
}, []);
```

### "Приложение не запускается"

**Чеклист:**
1. Node.js версия ≥ 18? → `node --version`
2. Зависимости установлены? → `npm install`
3. Порт свободен? → См. SETUP_GUIDE.md
4. Нет ошибок в консоли? → Покажи ошибку

### "Как добавить новый шаблон резюме?"

**Шаги:**
1. Создай компонент `ResumeTemplateClassic.jsx`
2. Скопируй структуру из `ResumePreview` (текущий Modern)
3. Измени стили (более консервативные для Classic)
4. Добавь переключатель шаблонов в UI
5. Используй Context/State для хранения выбранного шаблона

---

## 🧠 Контекст для разных задач

### При работе с формами (EditorForm):

**Читай:**
- App.jsx строки 208-404
- CONTEXT.md секция "Структура Master Profile"

**Помни:**
- Все изменения immutable
- onChange поднимается в App
- Live preview обновляется автоматически

### При работе с превью (ResumePreview):

**Читай:**
- App.jsx строки 128-206
- RULES.md секция "Resume Preview (A4 формат)"

**Помни:**
- Строгий A4: width: 210mm, min-height: 297mm, padding: 15mm
- Шрифт: Inter
- Это НЕ редактируемый компонент (только отображение)

### При работе с AI функциями:

**Читай:**
- App.jsx строки 509-532 (handleAIAction)
- CONTEXT.md секция "AI интеграция"
- ROADMAP.md "Фаза 2: AI Integration"

**Помни:**
- Сейчас моки - это нормально для MVP
- Реальная интеграция требует backend
- Токены должны подсчитываться

---

## 📊 Приоритеты (что делать в первую очередь)

### P0 (Критично для MVP):
1. Экспорт в PDF
2. LocalStorage (автосохранение)
3. Добавление/удаление блоков опыта

### P1 (Важно для UX):
4. Валидация форм
5. Индикаторы ошибок
6. Улучшение accessibility

### P2 (Nice to have):
7. Дополнительные шаблоны
8. Drag & drop для переупорядочивания
9. История изменений (undo/redo)

### P3 (Будущее):
10. Реальная AI интеграция
11. Backend + БД
12. Авторизация

См. полный ROADMAP.md

---

## 🛠️ Полезные команды

```bash
# Запуск dev-сервера
npm run dev

# Проверка lint ошибок
npm run lint

# Автофикс lint
npm run lint -- --fix

# Production build
npm run build

# Просмотр структуры компонентов
grep -r "const.*= (" App.jsx
```

---

## 🚨 Чего НЕ делать (Anti-patterns)

### ❌ Не мутируй состояние напрямую

```javascript
// НЕТ!
profileData.experience[0].description = "new";
setProfileData(profileData);

// ДА!
setProfileData(prev => ({
  ...prev,
  experience: prev.experience.map((exp, i) =>
    i === 0 ? { ...exp, description: "new" } : exp
  )
}));
```

### ❌ Не используй классовые компоненты

```javascript
// НЕТ!
class MyComponent extends React.Component { ... }

// ДА!
const MyComponent = () => { ... }
```

### ❌ Не пиши inline styles для статики

```javascript
// НЕТ!
<div style={{ padding: '10px', margin: '5px' }}>

// ДА!
<div className="p-2.5 m-1.5">
```

### ❌ Не игнорируй существующую структуру

Если в проекте используется определенный паттерн (например, все компоненты в App.jsx), НЕ создавай новую структуру без согласования.

### ❌ Не добавляй зависимости без необходимости

Если задачу можно решить существующими инструментами (React, Tailwind), не нужно ставить новые библиотеки.

---

## 💡 Советы по взаимодействию с пользователем

### Когда задача неясна:

❌ Плохо: "Я не понял, что ты хочешь"
✅ Хорошо: "Я могу помочь с этим двумя способами:
1. Добавить кнопку удаления для каждого блока опыта
2. Добавить множественный выбор с одной кнопкой удаления
Какой вариант предпочтительнее?"

### Когда нужно объяснить концепцию:

❌ Плохо: Сложные технические термины
✅ Хорошо: "Мастер-профиль - это как твой полный CV, который ты заполняешь один раз. Потом система автоматически адаптирует его под разные вакансии."

### Когда задача большая:

❌ Плохо: Сделать всё сразу
✅ Хорошо: "Эта задача состоит из 3 шагов:
1. Сначала добавим UI элементы
2. Потом добавим логику
3. Затем стилизуем
Начнем с первого?"

### Когда что-то сломалось:

❌ Плохо: "Ошибка в твоем коде"
✅ Хорошо: "Похоже, проблема в том, что мы обновляем состояние напрямую. Давай исправим на immutable подход."

---

## 📋 Чеклист перед завершением задачи

- [ ] Код следует RULES.md code style
- [ ] Нет мутаций состояния
- [ ] Используется Tailwind (не inline styles)
- [ ] Компоненты функциональные (не классовые)
- [ ] Нет lint ошибок (`npm run lint`)
- [ ] Live preview работает корректно
- [ ] Изменения не сломали существующую функциональность
- [ ] Если добавлена фича из ROADMAP - отметить как completed

---

## 🎓 Дополнительное обучение

### Изучи эти паттерны в App.jsx:

1. **Lifting State Up** (строки 503-532)
   - Состояние в App, передается вниз через props

2. **Controlled Components** (строки 267-310)
   - Input контролируется через value + onChange

3. **Conditional Rendering** (строки 542-577)
   - Split-view vs Job Tailoring view

4. **Component Composition** (строки 60-110)
   - Sidebar с массивом menuItems

### React хуки в проекте:

- `useState` - локальное состояние
- `useEffect` - side effects (пока не используется, но будет для API)
- `useMemo` - оптимизация (планируется)
- `useCallback` - оптимизация колбэков (планируется)

---

## 🎯 Итого: Твоя роль как AI-ассистента

1. **Понимай контекст** - читай CONTEXT.md
2. **Следуй правилам** - соблюдай RULES.md
3. **Знай план** - изучай ROADMAP.md
4. **Пиши качественный код** - используй best practices
5. **Объясняй понятно** - пользователь может быть не экспертом
6. **Предлагай улучшения** - если видишь возможность оптимизации

**Главное правило:**
Перед написанием кода всегда сначала ПРОЧИТАЙ соответствующие разделы документации.

---

*Этот документ будет обновляться по мере развития проекта.*

*Последнее обновление: 25 ноября 2025*

